<?php
// This script and data application was generated by AppGini, https://bigprof.com/appgini
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/agenda_table.php');
	include_once(__DIR__ . '/agenda_table_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('agenda_table');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'agenda_table';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`agenda_table`.`agenda_id`" => "agenda_id",
		"IF(    CHAR_LENGTH(`meetings_table1`.`meeting_str`), CONCAT_WS('',   `meetings_table1`.`meeting_str`), '') /* Meeting */" => "meeting_lookup",
		"`agenda_table`.`agenda_description`" => "agenda_description",
		"`agenda_table`.`agenda_str`" => "agenda_str",
		"`agenda_table`.`created_by`" => "created_by",
		"`agenda_table`.`last_updated_by`" => "last_updated_by",
		"`agenda_table`.`created_by_username`" => "created_by_username",
		"`agenda_table`.`created_at`" => "created_at",
		"`agenda_table`.`last_updated_by_username`" => "last_updated_by_username",
		"`agenda_table`.`last_updated_at`" => "last_updated_at",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`agenda_table`.`agenda_id`',
		2 => '`meetings_table1`.`meeting_str`',
		3 => 3,
		4 => 4,
		5 => 5,
		6 => 6,
		7 => 7,
		8 => 8,
		9 => 9,
		10 => 10,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`agenda_table`.`agenda_id`" => "agenda_id",
		"IF(    CHAR_LENGTH(`meetings_table1`.`meeting_str`), CONCAT_WS('',   `meetings_table1`.`meeting_str`), '') /* Meeting */" => "meeting_lookup",
		"`agenda_table`.`agenda_description`" => "agenda_description",
		"`agenda_table`.`agenda_str`" => "agenda_str",
		"`agenda_table`.`created_by`" => "created_by",
		"`agenda_table`.`last_updated_by`" => "last_updated_by",
		"`agenda_table`.`created_by_username`" => "created_by_username",
		"`agenda_table`.`created_at`" => "created_at",
		"`agenda_table`.`last_updated_by_username`" => "last_updated_by_username",
		"`agenda_table`.`last_updated_at`" => "last_updated_at",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`agenda_table`.`agenda_id`" => "ID",
		"IF(    CHAR_LENGTH(`meetings_table1`.`meeting_str`), CONCAT_WS('',   `meetings_table1`.`meeting_str`), '') /* Meeting */" => "Meeting",
		"`agenda_table`.`agenda_description`" => "Agenda description",
		"`agenda_table`.`agenda_str`" => "Agenda str",
		"`agenda_table`.`created_by`" => "Created By",
		"`agenda_table`.`last_updated_by`" => "Last Updated By",
		"`agenda_table`.`created_by_username`" => "Created By Username",
		"`agenda_table`.`created_at`" => "Created At",
		"`agenda_table`.`last_updated_by_username`" => "Last Updated by Username",
		"`agenda_table`.`last_updated_at`" => "Last Updated At",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`agenda_table`.`agenda_id`" => "agenda_id",
		"IF(    CHAR_LENGTH(`meetings_table1`.`meeting_str`), CONCAT_WS('',   `meetings_table1`.`meeting_str`), '') /* Meeting */" => "meeting_lookup",
		"`agenda_table`.`agenda_description`" => "agenda_description",
		"`agenda_table`.`agenda_str`" => "agenda_str",
		"`agenda_table`.`created_by`" => "created_by",
		"`agenda_table`.`last_updated_by`" => "last_updated_by",
		"`agenda_table`.`created_by_username`" => "created_by_username",
		"`agenda_table`.`created_at`" => "created_at",
		"`agenda_table`.`last_updated_by_username`" => "last_updated_by_username",
		"`agenda_table`.`last_updated_at`" => "last_updated_at",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = ['meeting_lookup' => 'Meeting', ];

	$x->QueryFrom = "`agenda_table` LEFT JOIN `meetings_table` as meetings_table1 ON `meetings_table1`.`meetings_id`=`agenda_table`.`meeting_lookup` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->AllowAdminShowSQL = showSQL();
	$x->RecordsPerPage = 100;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'agenda_table_view.php';
	$x->TableTitle = 'Agenda - App';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`agenda_table`.`agenda_id`';

	$x->ColWidth = [150, 150, 150, 150, 150, 150, 150, 150, 150, ];
	$x->ColCaption = ['ID', 'Meeting', 'Agenda description', 'Created By', 'Last Updated By', 'Created By Username', 'Created At', 'Last Updated by Username', 'Last Updated At', ];
	$x->ColFieldName = ['agenda_id', 'meeting_lookup', 'agenda_description', 'created_by', 'last_updated_by', 'created_by_username', 'created_at', 'last_updated_by_username', 'last_updated_at', ];
	$x->ColNumber  = [1, 2, 3, 5, 6, 7, 8, 9, 10, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/agenda_table_templateTV.html';
	$x->SelectedTemplate = 'templates/agenda_table_templateTVS.html';
	$x->TemplateDV = 'templates/agenda_table_templateDV.html';
	$x->TemplateDVP = 'templates/agenda_table_templateDVP.html';

	$x->ShowTableHeader = 0;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = true;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: agenda_table_init
	$render = true;
	if(function_exists('agenda_table_init')) {
		$args = [];
		$render = agenda_table_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: agenda_table_header
	$headerCode = '';
	if(function_exists('agenda_table_header')) {
		$args = [];
		$headerCode = agenda_table_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php');
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: agenda_table_footer
	$footerCode = '';
	if(function_exists('agenda_table_footer')) {
		$args = [];
		$footerCode = agenda_table_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php');
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
