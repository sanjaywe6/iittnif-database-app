<?php

// Data functions (insert, update, delete, form) for table ed_gcc

// This script and data application was generated by AppGini, https://bigprof.com/appgini
// Download AppGini for free from https://bigprof.com/appgini/download/

function ed_gcc_insert(&$error_message = '') {
	global $Translation;

	// mm: can member insert record?
	$arrPerm = getTablePermissions('ed_gcc');
	if(!$arrPerm['insert']) {
		$error_message = $Translation['no insert permission'];
		return false;
	}

	$data = [
		'year' => Request::val('year', '2020-21'),
		'name' => Request::val('name', ''),
		'finalists' => Request::val('finalists', ''),
		'Number_of_Enrolled_Participants' => Request::val('Number_of_Enrolled_Participants', ''),
		'Technology_Domain' => Request::val('Technology_Domain', ''),
		'Start_Date' => Request::dateComponents('Start_Date', ''),
		'End_Date' => Request::dateComponents('End_Date', ''),
		'status' => Request::val('status', ''),
		'Co_Sponsors' => Request::val('Co_Sponsors', ''),
		'created_at' => parseCode('<%%creationDateTime%%>', true),
		'created_by' => parseCode('<%%creatorUsername%%>', true),
	];

	// record owner is current user
	$recordOwner = getLoggedMemberID();

	$recID = tableInsert('ed_gcc', $data, $recordOwner, $error_message);

	// if this record is a copy of another record, copy children if applicable
	if(strlen(Request::val('SelectedID')) && $recID !== false)
		ed_gcc_copy_children($recID, Request::val('SelectedID'));

	return $recID;
}

function ed_gcc_copy_children($destination_id, $source_id) {
	global $Translation;
	$requests = []; // array of curl handlers for launching insert requests
	$eo = ['silentErrors' => true];
	$safe_sid = makeSafe($source_id);
	$currentUsername = getLoggedMemberID();
	$errorMessage = '';

	// launch requests, asynchronously
	curl_batch($requests);
}

function ed_gcc_delete($selected_id, $AllowDeleteOfParents = false, $skipChecks = false) {
	// insure referential integrity ...
	global $Translation;
	$selected_id = makeSafe($selected_id);

	// mm: can member delete record?
	if(!check_record_permission('ed_gcc', $selected_id, 'delete')) {
		return $Translation['You don\'t have enough permissions to delete this record'];
	}

	// hook: ed_gcc_before_delete
	if(function_exists('ed_gcc_before_delete')) {
		$args = [];
		if(!ed_gcc_before_delete($selected_id, $skipChecks, getMemberInfo(), $args))
			return $Translation['Couldn\'t delete this record'] . (
				!empty($args['error_message']) ?
					'<div class="text-bold">' . strip_tags($args['error_message']) . '</div>'
					: ''
			);
	}

	sql("DELETE FROM `ed_gcc` WHERE `id`='{$selected_id}'", $eo);

	// hook: ed_gcc_after_delete
	if(function_exists('ed_gcc_after_delete')) {
		$args = [];
		ed_gcc_after_delete($selected_id, getMemberInfo(), $args);
	}

	// mm: delete ownership data
	sql("DELETE FROM `membership_userrecords` WHERE `tableName`='ed_gcc' AND `pkValue`='{$selected_id}'", $eo);
}

function ed_gcc_update(&$selected_id, &$error_message = '') {
	global $Translation;

	// mm: can member edit record?
	if(!check_record_permission('ed_gcc', $selected_id, 'edit')) return false;

	$data = [
		'year' => Request::val('year', ''),
		'name' => Request::val('name', ''),
		'finalists' => Request::val('finalists', ''),
		'Number_of_Enrolled_Participants' => Request::val('Number_of_Enrolled_Participants', ''),
		'Technology_Domain' => Request::val('Technology_Domain', ''),
		'Start_Date' => Request::dateComponents('Start_Date', ''),
		'End_Date' => Request::dateComponents('End_Date', ''),
		'status' => Request::val('status', ''),
		'Co_Sponsors' => Request::val('Co_Sponsors', ''),
		'last_updated_at' => parseCode('<%%editingDateTime%%>', false),
		'last_updated_by' => parseCode('<%%editorUsername%%>', false),
	];

	if($data['year'] === '') {
		echo StyleSheet() . "\n\n<div class=\"alert alert-danger\">{$Translation['error:']} 'Year': {$Translation['field not null']}<br><br>";
		echo '<a href="" onclick="history.go(-1); return false;">' . $Translation['< back'] . '</a></div>';
		exit;
	}
	if($data['name'] === '') {
		echo StyleSheet() . "\n\n<div class=\"alert alert-danger\">{$Translation['error:']} 'Name of Competition': {$Translation['field not null']}<br><br>";
		echo '<a href="" onclick="history.go(-1); return false;">' . $Translation['< back'] . '</a></div>';
		exit;
	}
	// get existing values
	$old_data = getRecord('ed_gcc', $selected_id);
	if(is_array($old_data)) {
		$old_data = array_map('makeSafe', $old_data);
		$old_data['selectedID'] = makeSafe($selected_id);
	}

	$data['selectedID'] = makeSafe($selected_id);

	// hook: ed_gcc_before_update
	if(function_exists('ed_gcc_before_update')) {
		$args = ['old_data' => $old_data];
		if(!ed_gcc_before_update($data, getMemberInfo(), $args)) {
			if(isset($args['error_message'])) $error_message = $args['error_message'];
			return false;
		}
	}

	$set = $data; unset($set['selectedID']);
	foreach ($set as $field => $value) {
		$set[$field] = ($value !== '' && $value !== NULL) ? $value : NULL;
	}

	if(!update(
		'ed_gcc',
		backtick_keys_once($set),
		['`id`' => $selected_id],
		$error_message
	)) {
		echo $error_message;
		echo '<a href="ed_gcc_view.php?SelectedID=' . urlencode($selected_id) . "\">{$Translation['< back']}</a>";
		exit;
	}


	update_calc_fields('ed_gcc', $data['selectedID'], calculated_fields()['ed_gcc']);

	// hook: ed_gcc_after_update
	if(function_exists('ed_gcc_after_update')) {
		if($row = getRecord('ed_gcc', $data['selectedID'])) $data = array_map('makeSafe', $row);

		$data['selectedID'] = $data['id'];
		$args = ['old_data' => $old_data];
		if(!ed_gcc_after_update($data, getMemberInfo(), $args)) return;
	}

	// mm: update record update timestamp
	set_record_owner('ed_gcc', $selected_id);
}

function ed_gcc_form($selectedId = '', $allowUpdate = true, $allowInsert = true, $allowDelete = true, $separateDV = true, $templateDV = '', $templateDVP = '') {
	// function to return an editable form for a table records
	// and fill it with data of record whose ID is $selectedId. If $selectedId
	// is empty, an empty form is shown, with only an 'Add New'
	// button displayed.

	global $Translation;
	$eo = ['silentErrors' => true];
	$noUploads = $row = $urow = $jsReadOnly = $jsEditable = $lookups = null;
	$noSaveAsCopy = true;
	$hasSelectedId = strlen($selectedId) > 0;

	// mm: get table permissions
	$arrPerm = getTablePermissions('ed_gcc');
	$allowInsert = ($arrPerm['insert'] ? true : false);
	$allowUpdate = $hasSelectedId && check_record_permission('ed_gcc', $selectedId, 'edit');
	$allowDelete = $hasSelectedId && check_record_permission('ed_gcc', $selectedId, 'delete');

	if(!$allowInsert && !$hasSelectedId)
		// no insert permission and no record selected
		// so show access denied error -- except if TVDV: just hide DV
		return $separateDV ? $Translation['tableAccessDenied'] : '';

	if($hasSelectedId && !check_record_permission('ed_gcc', $selectedId, 'view'))
		return $Translation['tableAccessDenied'];

	// print preview?
	$dvprint = $hasSelectedId && Request::val('dvprint_x') != '';

	$showSaveNew = !$dvprint && ($allowInsert && !$hasSelectedId);
	$showSaveChanges = !$dvprint && $allowUpdate && $hasSelectedId;
	$showDelete = !$dvprint && $allowDelete && $hasSelectedId;
	$showSaveAsCopy = !$dvprint && ($allowInsert && $hasSelectedId && !$noSaveAsCopy);
	$fieldsAreEditable = !$dvprint && (($allowInsert && !$hasSelectedId) || ($allowUpdate && $hasSelectedId) || $showSaveAsCopy);


	// populate filterers, starting from children to grand-parents

	// unique random identifier
	$rnd1 = ($dvprint ? rand(1000000, 9999999) : '');
	// combobox: year
	$combo_year = new Combo;
	$combo_year->ListType = 0;
	$combo_year->MultipleSeparator = ', ';
	$combo_year->ListBoxHeight = 10;
	$combo_year->RadiosPerLine = 1;
	if(is_file(__DIR__ . '/hooks/ed_gcc.year.csv')) {
		$year_data = addslashes(implode('', @file(__DIR__ . '/hooks/ed_gcc.year.csv')));
		$combo_year->ListItem = array_trim(explode('||', entitiesToUTF8(convertLegacyOptions($year_data))));
		$combo_year->ListData = $combo_year->ListItem;
	} else {
		$combo_year->ListItem = array_trim(explode('||', entitiesToUTF8(convertLegacyOptions("2020-21;;2021-22;;2022-23;;2023-24;;2024-25;;2025-26;;2026-27;;2027-28"))));
		$combo_year->ListData = $combo_year->ListItem;
	}
	$combo_year->SelectName = 'year';
	$combo_year->AllowNull = false;
	// combobox: Start_Date
	$combo_Start_Date = new DateCombo;
	$combo_Start_Date->DateFormat = "dmy";
	$combo_Start_Date->MinYear = defined('ed_gcc.Start_Date.MinYear') ? constant('ed_gcc.Start_Date.MinYear') : 1900;
	$combo_Start_Date->MaxYear = defined('ed_gcc.Start_Date.MaxYear') ? constant('ed_gcc.Start_Date.MaxYear') : 2100;
	$combo_Start_Date->DefaultDate = parseMySQLDate('', '');
	$combo_Start_Date->MonthNames = $Translation['month names'];
	$combo_Start_Date->NamePrefix = 'Start_Date';
	// combobox: End_Date
	$combo_End_Date = new DateCombo;
	$combo_End_Date->DateFormat = "dmy";
	$combo_End_Date->MinYear = defined('ed_gcc.End_Date.MinYear') ? constant('ed_gcc.End_Date.MinYear') : 1900;
	$combo_End_Date->MaxYear = defined('ed_gcc.End_Date.MaxYear') ? constant('ed_gcc.End_Date.MaxYear') : 2100;
	$combo_End_Date->DefaultDate = parseMySQLDate('', '');
	$combo_End_Date->MonthNames = $Translation['month names'];
	$combo_End_Date->NamePrefix = 'End_Date';
	// combobox: status
	$combo_status = new Combo;
	$combo_status->ListType = 0;
	$combo_status->MultipleSeparator = ', ';
	$combo_status->ListBoxHeight = 10;
	$combo_status->RadiosPerLine = 1;
	if(is_file(__DIR__ . '/hooks/ed_gcc.status.csv')) {
		$status_data = addslashes(implode('', @file(__DIR__ . '/hooks/ed_gcc.status.csv')));
		$combo_status->ListItem = array_trim(explode('||', entitiesToUTF8(convertLegacyOptions($status_data))));
		$combo_status->ListData = $combo_status->ListItem;
	} else {
		$combo_status->ListItem = array_trim(explode('||', entitiesToUTF8(convertLegacyOptions("Ongoing;;Closed;;Completed"))));
		$combo_status->ListData = $combo_status->ListItem;
	}
	$combo_status->SelectName = 'status';

	if($hasSelectedId) {
		if(!($row = getRecord('ed_gcc', $selectedId))) {
			return error_message($Translation['No records found'], 'ed_gcc_view.php', false);
		}
		$combo_year->SelectedData = $row['year'];
		$combo_Start_Date->DefaultDate = $row['Start_Date'];
		$combo_End_Date->DefaultDate = $row['End_Date'];
		$combo_status->SelectedData = $row['status'];
		$urow = $row; /* unsanitized data */
		$row = array_map('safe_html', $row);
	} else {
		$filterField = Request::val('FilterField');
		$filterOperator = Request::val('FilterOperator');
		$filterValue = Request::val('FilterValue');
		$combo_year->SelectedText = (isset($filterField[1]) && $filterField[1] == '2' && $filterOperator[1] == '<=>' ? $filterValue[1] : entitiesToUTF8('2020-21'));
		$combo_status->SelectedText = (isset($filterField[1]) && $filterField[1] == '9' && $filterOperator[1] == '<=>' ? $filterValue[1] : entitiesToUTF8(''));
	}
	$combo_year->Render();
	$combo_status->Render();

	ob_start();
	?>

	<script>
		// initial lookup values

		$j(function() {
			setTimeout(function() {
			}, 50); /* we need to slightly delay client-side execution of the above code to allow AppGini.ajaxCache to work */
		});
	</script>
	<?php

	$lookups = str_replace('__RAND__', $rnd1, ob_get_clean());


	// code for template based detail view forms

	// open the detail view template
	if($dvprint) {
		$template_file = is_file("./{$templateDVP}") ? "./{$templateDVP}" : './templates/ed_gcc_templateDVP.html';
		$templateCode = @file_get_contents($template_file);
	} else {
		$template_file = is_file("./{$templateDV}") ? "./{$templateDV}" : './templates/ed_gcc_templateDV.html';
		$templateCode = @file_get_contents($template_file);
	}

	// process form title
	$templateCode = str_replace('<%%DETAIL_VIEW_TITLE%%>', 'Ed gcc details', $templateCode);
	$templateCode = str_replace('<%%RND1%%>', $rnd1, $templateCode);
	$templateCode = str_replace('<%%EMBEDDED%%>', (Request::val('Embedded') ? 'Embedded=1' : ''), $templateCode);
	// process buttons
	if($showSaveNew) {
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '<button type="submit" class="btn btn-success" id="insert" name="insert_x" value="1"><i class="glyphicon glyphicon-plus-sign"></i> ' . $Translation['Save New'] . '</button>', $templateCode);
	} elseif($showSaveAsCopy) {
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="insert" name="insert_x" value="1"><i class="glyphicon glyphicon-plus-sign"></i> ' . $Translation['Save As Copy'] . '</button>', $templateCode);
	} else {
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '', $templateCode);
	}

	// 'Back' button action
	if(Request::val('Embedded')) {
		$backAction = 'AppGini.closeParentModal(); return false;';
	} else {
		$backAction = 'return true;';
	}

	if($hasSelectedId) {
		if(!Request::val('Embedded')) $templateCode = str_replace('<%%DVPRINT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="dvprint" name="dvprint_x" value="1" title="' . html_attr($Translation['Print Preview']) . '"><i class="glyphicon glyphicon-print"></i> ' . $Translation['Print Preview'] . '</button>', $templateCode);
		if($allowUpdate)
			$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '<button type="submit" class="btn btn-success btn-lg" id="update" name="update_x" value="1" title="' . html_attr($Translation['Save Changes']) . '"><i class="glyphicon glyphicon-ok"></i> ' . $Translation['Save Changes'] . '</button>', $templateCode);
		else
			$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '', $templateCode);

		if($allowDelete)
			$templateCode = str_replace('<%%DELETE_BUTTON%%>', '<button type="submit" class="btn btn-danger" id="delete" name="delete_x" value="1" title="' . html_attr($Translation['Delete']) . '"><i class="glyphicon glyphicon-trash"></i> ' . $Translation['Delete'] . '</button>', $templateCode);
		else
			$templateCode = str_replace('<%%DELETE_BUTTON%%>', '', $templateCode);

		$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '<button type="submit" class="btn btn-default ltr" id="deselect" name="deselect_x" value="1" onclick="' . $backAction . '" title="' . html_attr($Translation['Back']) . '"><i class="glyphicon glyphicon-chevron-left"></i> ' . $Translation['Back'] . '</button>', $templateCode);
	} else {
		$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '', $templateCode);
		$templateCode = str_replace('<%%DELETE_BUTTON%%>', '', $templateCode);

		// if not in embedded mode and user has insert only but no view/update/delete,
		// remove 'back' button
		if(
			$allowInsert
			&& !$allowUpdate && !$allowDelete && !$arrPerm['view']
			&& !Request::val('Embedded')
		)
			$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '', $templateCode);
		elseif($separateDV)
			$templateCode = str_replace(
				'<%%DESELECT_BUTTON%%>',
				'<button
					type="submit"
					class="btn btn-default ltr"
					id="deselect"
					name="deselect_x"
					value="1"
					onclick="' . $backAction . '"
					title="' . html_attr($Translation['Back']) . '">
						<i class="glyphicon glyphicon-chevron-left"></i> ' .
						$Translation['Back'] .
				'</button>',
				$templateCode
			);
		else
			$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '', $templateCode);
	}

	// set records to read only if user can't insert new records and can't edit current record
	if(!$fieldsAreEditable) {
		$jsReadOnly = '';
		$jsReadOnly .= "\t\$j('#year').replaceWith('<div class=\"form-control-static\" id=\"year\">' + (\$j('#year').val() || '') + '</div>'); \$j('#year-multi-selection-help').hide();\n";
		$jsReadOnly .= "\t\$j('#name').replaceWith('<div class=\"form-control-static\" id=\"name\">' + (\$j('#name').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('#finalists').replaceWith('<div class=\"form-control-static\" id=\"finalists\">' + (\$j('#finalists').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('#Number_of_Enrolled_Participants').replaceWith('<div class=\"form-control-static\" id=\"Number_of_Enrolled_Participants\">' + (\$j('#Number_of_Enrolled_Participants').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('#Technology_Domain').replaceWith('<div class=\"form-control-static\" id=\"Technology_Domain\">' + (\$j('#Technology_Domain').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('#Start_Date').prop('readonly', true);\n";
		$jsReadOnly .= "\t\$j('#Start_DateDay, #Start_DateMonth, #Start_DateYear').prop('disabled', true).css({ color: '#555', backgroundColor: '#fff' });\n";
		$jsReadOnly .= "\t\$j('#End_Date').prop('readonly', true);\n";
		$jsReadOnly .= "\t\$j('#End_DateDay, #End_DateMonth, #End_DateYear').prop('disabled', true).css({ color: '#555', backgroundColor: '#fff' });\n";
		$jsReadOnly .= "\t\$j('#status').replaceWith('<div class=\"form-control-static\" id=\"status\">' + (\$j('#status').val() || '') + '</div>'); \$j('#status-multi-selection-help').hide();\n";
		$jsReadOnly .= "\t\$j('#Co_Sponsors').replaceWith('<div class=\"form-control-static\" id=\"Co_Sponsors\">' + (\$j('#Co_Sponsors').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('.select2-container').hide();\n";

		$noUploads = true;
	} else {
		// temporarily disable form change handler till time and datetime pickers are enabled
		$jsEditable = "\t\$j('form').eq(0).data('already_changed', true);";
		$jsEditable .= "\t\$j('form').eq(0).data('already_changed', false);"; // re-enable form change handler
	}

	// process combos
	$templateCode = str_replace('<%%COMBO(year)%%>', $combo_year->HTML, $templateCode);
	$templateCode = str_replace('<%%COMBOTEXT(year)%%>', $combo_year->SelectedData, $templateCode);
	$templateCode = str_replace(
		'<%%COMBO(Start_Date)%%>',
		(!$fieldsAreEditable ?
			'<div class="form-control-static">' . $combo_Start_Date->GetHTML(true) . '</div>' :
			$combo_Start_Date->GetHTML()
		), $templateCode);
	$templateCode = str_replace('<%%COMBOTEXT(Start_Date)%%>', $combo_Start_Date->GetHTML(true), $templateCode);
	$templateCode = str_replace(
		'<%%COMBO(End_Date)%%>',
		(!$fieldsAreEditable ?
			'<div class="form-control-static">' . $combo_End_Date->GetHTML(true) . '</div>' :
			$combo_End_Date->GetHTML()
		), $templateCode);
	$templateCode = str_replace('<%%COMBOTEXT(End_Date)%%>', $combo_End_Date->GetHTML(true), $templateCode);
	$templateCode = str_replace('<%%COMBO(status)%%>', $combo_status->HTML, $templateCode);
	$templateCode = str_replace('<%%COMBOTEXT(status)%%>', $combo_status->SelectedData, $templateCode);

	/* lookup fields array: 'lookup field name' => ['parent table name', 'lookup field caption'] */
	$lookup_fields = [];
	foreach($lookup_fields as $luf => $ptfc) {
		$pt_perm = getTablePermissions($ptfc[0]);

		// process foreign key links
		if(($pt_perm['view'] && isDetailViewEnabled($ptfc[0])) || $pt_perm['edit']) {
			$templateCode = str_replace("<%%PLINK({$luf})%%>", '<button type="button" class="btn btn-default view_parent" id="' . $ptfc[0] . '_view_parent" title="' . html_attr($Translation['View'] . ' ' . $ptfc[1]) . '"><i class="glyphicon glyphicon-eye-open"></i></button>', $templateCode);
		}

		// if user has insert permission to parent table of a lookup field, put an add new button
		if($pt_perm['insert'] /* && !Request::val('Embedded')*/) {
			$templateCode = str_replace("<%%ADDNEW({$ptfc[0]})%%>", '<button type="button" class="btn btn-default add_new_parent" id="' . $ptfc[0] . '_add_new" title="' . html_attr($Translation['Add New'] . ' ' . $ptfc[1]) . '"><i class="glyphicon glyphicon-plus text-success"></i></button>', $templateCode);
		}
	}

	// process images
	$templateCode = str_replace('<%%UPLOADFILE(id)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(year)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(name)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(finalists)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(Number_of_Enrolled_Participants)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(Technology_Domain)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(Start_Date)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(End_Date)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(status)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(Co_Sponsors)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(created_by_username)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(created_at)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(last_updated_by_username)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(last_updated_at)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(last_updated_by)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(created_by)%%>', '', $templateCode);

	// process values
	if($hasSelectedId) {
		if( $dvprint) $templateCode = str_replace('<%%VALUE(id)%%>', safe_html($urow['id']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(id)%%>', html_attr($row['id']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(id)%%>', urlencode($urow['id']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(year)%%>', safe_html($urow['year']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(year)%%>', html_attr($row['year']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(year)%%>', urlencode($urow['year']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(name)%%>', safe_html($urow['name']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(name)%%>', html_attr($row['name']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(name)%%>', urlencode($urow['name']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(finalists)%%>', safe_html($urow['finalists']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(finalists)%%>', html_attr($row['finalists']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(finalists)%%>', urlencode($urow['finalists']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(Number_of_Enrolled_Participants)%%>', safe_html($urow['Number_of_Enrolled_Participants']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(Number_of_Enrolled_Participants)%%>', html_attr($row['Number_of_Enrolled_Participants']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(Number_of_Enrolled_Participants)%%>', urlencode($urow['Number_of_Enrolled_Participants']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(Technology_Domain)%%>', safe_html($urow['Technology_Domain']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(Technology_Domain)%%>', html_attr($row['Technology_Domain']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(Technology_Domain)%%>', urlencode($urow['Technology_Domain']), $templateCode);
		$templateCode = str_replace('<%%VALUE(Start_Date)%%>', app_datetime($row['Start_Date']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(Start_Date)%%>', urlencode(app_datetime($urow['Start_Date'])), $templateCode);
		$templateCode = str_replace('<%%VALUE(End_Date)%%>', app_datetime($row['End_Date']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(End_Date)%%>', urlencode(app_datetime($urow['End_Date'])), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(status)%%>', safe_html($urow['status']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(status)%%>', html_attr($row['status']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(status)%%>', urlencode($urow['status']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(Co_Sponsors)%%>', safe_html($urow['Co_Sponsors']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(Co_Sponsors)%%>', html_attr($row['Co_Sponsors']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(Co_Sponsors)%%>', urlencode($urow['Co_Sponsors']), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_by_username)%%>', safe_html($urow['created_by_username']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_by_username)%%>', urlencode($urow['created_by_username']), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_at)%%>', safe_html($urow['created_at']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_at)%%>', urlencode($urow['created_at']), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_by_username)%%>', safe_html($urow['last_updated_by_username']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_by_username)%%>', urlencode($urow['last_updated_by_username']), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_at)%%>', safe_html($urow['last_updated_at']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_at)%%>', urlencode($urow['last_updated_at']), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_by)%%>', safe_html($urow['last_updated_by']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_by)%%>', urlencode($urow['last_updated_by']), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_by)%%>', safe_html($urow['created_by']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_by)%%>', urlencode($urow['created_by']), $templateCode);
	} else {
		$templateCode = str_replace('<%%VALUE(id)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(id)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(year)%%>', '2020-21', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(year)%%>', urlencode('2020-21'), $templateCode);
		$templateCode = str_replace('<%%VALUE(name)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(name)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(finalists)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(finalists)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(Number_of_Enrolled_Participants)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(Number_of_Enrolled_Participants)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(Technology_Domain)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(Technology_Domain)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(Start_Date)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(Start_Date)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(End_Date)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(End_Date)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(status)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(status)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(Co_Sponsors)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(Co_Sponsors)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_by_username)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_by_username)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_at)%%>', '<%%creationDateTime%%>', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_at)%%>', urlencode('<%%creationDateTime%%>'), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_by_username)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_by_username)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_at)%%>', '<%%editingDateTime%%>', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_at)%%>', urlencode('<%%editingDateTime%%>'), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_by)%%>', '<%%editorUsername%%>', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_by)%%>', urlencode('<%%editorUsername%%>'), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_by)%%>', '<%%creatorUsername%%>', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_by)%%>', urlencode('<%%creatorUsername%%>'), $templateCode);
	}

	// process translations
	$templateCode = parseTemplate($templateCode);

	// clear scrap
	$templateCode = str_replace('<%%', '<!-- ', $templateCode);
	$templateCode = str_replace('%%>', ' -->', $templateCode);

	// hide links to inaccessible tables
	if(Request::val('dvprint_x') == '') {
		$templateCode .= "\n\n<script>\$j(function() {\n";
		$arrTables = getTableList();
		foreach($arrTables as $name => $caption) {
			$templateCode .= "\t\$j('#{$name}_link').removeClass('hidden');\n";
			$templateCode .= "\t\$j('#xs_{$name}_link').removeClass('hidden');\n";
		}

		$templateCode .= $jsReadOnly;
		$templateCode .= $jsEditable;

		if(!$hasSelectedId) {
		}

		$templateCode.="\n});</script>\n";
	}

	// ajaxed auto-fill fields
	$templateCode .= '<script>';
	$templateCode .= '$j(function() {';


	$templateCode.="});";
	$templateCode.="</script>";
	$templateCode .= $lookups;

	// handle enforced parent values for read-only lookup fields
	$filterField = Request::val('FilterField');
	$filterOperator = Request::val('FilterOperator');
	$filterValue = Request::val('FilterValue');

	// don't include blank images in lightbox gallery
	$templateCode = preg_replace('/blank.gif" data-lightbox=".*?"/', 'blank.gif"', $templateCode);

	// don't display empty email links
	$templateCode=preg_replace('/<a .*?href="mailto:".*?<\/a>/', '', $templateCode);

	/* default field values */
	$rdata = $jdata = get_defaults('ed_gcc');
	if($hasSelectedId) {
		$jdata = get_joined_record('ed_gcc', $selectedId);
		if($jdata === false) $jdata = get_defaults('ed_gcc');
		$rdata = $row;
	}
	$templateCode .= loadView('ed_gcc-ajax-cache', ['rdata' => $rdata, 'jdata' => $jdata]);

	// hook: ed_gcc_dv
	if(function_exists('ed_gcc_dv')) {
		$args = [];
		ed_gcc_dv(($hasSelectedId ? $selectedId : FALSE), getMemberInfo(), $templateCode, $args);
	}

	return $templateCode;
}