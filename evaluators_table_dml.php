<?php

// Data functions (insert, update, delete, form) for table evaluators_table

// This script and data application was generated by AppGini, https://bigprof.com/appgini
// Download AppGini for free from https://bigprof.com/appgini/download/

function evaluators_table_insert(&$error_message = '') {
	global $Translation;

	// mm: can member insert record?
	$arrPerm = getTablePermissions('evaluators_table');
	if(!$arrPerm['insert']) {
		$error_message = $Translation['no insert permission'];
		return false;
	}

	// automatic evaluation_lookup if passed as filterer
	if(Request::val('filterer_evaluation_lookup')) {
		$_REQUEST['evaluation_lookup'] = Request::val('filterer_evaluation_lookup');
	}

	$data = [
		'name' => Request::val('name', ''),
		'designation' => Request::val('designation', ''),
		'qualification' => Request::val('qualification', ''),
		'self_description' => br2nl(Request::val('self_description', '')),
		'role' => Request::val('role', ''),
		'created_by' => parseCode('<%%creatorUsername%%>', true),
		'created_at' => parseCode('<%%creationDateTime%%>', true),
	];


	// automatic evaluation_lookup if passed as filterer
	if(Request::val('filterer_evaluation_lookup')) {
		$data['evaluation_lookup'] = Request::val('filterer_evaluation_lookup');
	}
	// record owner is current user
	$recordOwner = getLoggedMemberID();

	$recID = tableInsert('evaluators_table', $data, $recordOwner, $error_message);

	// if this record is a copy of another record, copy children if applicable
	if(strlen(Request::val('SelectedID')) && $recID !== false)
		evaluators_table_copy_children($recID, Request::val('SelectedID'));

	return $recID;
}

function evaluators_table_copy_children($destination_id, $source_id) {
	global $Translation;
	$requests = []; // array of curl handlers for launching insert requests
	$eo = ['silentErrors' => true];
	$safe_sid = makeSafe($source_id);
	$currentUsername = getLoggedMemberID();
	$errorMessage = '';

	// launch requests, asynchronously
	curl_batch($requests);
}

function evaluators_table_delete($selected_id, $AllowDeleteOfParents = false, $skipChecks = false) {
	// insure referential integrity ...
	global $Translation;
	$selected_id = makeSafe($selected_id);

	// mm: can member delete record?
	if(!check_record_permission('evaluators_table', $selected_id, 'delete')) {
		return $Translation['You don\'t have enough permissions to delete this record'];
	}

	// hook: evaluators_table_before_delete
	if(function_exists('evaluators_table_before_delete')) {
		$args = [];
		if(!evaluators_table_before_delete($selected_id, $skipChecks, getMemberInfo(), $args))
			return $Translation['Couldn\'t delete this record'] . (
				!empty($args['error_message']) ?
					'<div class="text-bold">' . strip_tags($args['error_message']) . '</div>'
					: ''
			);
	}

	sql("DELETE FROM `evaluators_table` WHERE `evaluator_id`='{$selected_id}'", $eo);

	// hook: evaluators_table_after_delete
	if(function_exists('evaluators_table_after_delete')) {
		$args = [];
		evaluators_table_after_delete($selected_id, getMemberInfo(), $args);
	}

	// mm: delete ownership data
	sql("DELETE FROM `membership_userrecords` WHERE `tableName`='evaluators_table' AND `pkValue`='{$selected_id}'", $eo);
}

function evaluators_table_update(&$selected_id, &$error_message = '') {
	global $Translation;

	// mm: can member edit record?
	if(!check_record_permission('evaluators_table', $selected_id, 'edit')) return false;

	// since evaluators_table.evaluation_lookup is hidden in DV, force its stored value rather than submitted value
	$_REQUEST['evaluation_lookup'] = sqlValue("SELECT `evaluation_lookup` FROM `evaluators_table` WHERE `evaluator_id`='" . makeSafe($selected_id) . "'");

	$data = [
		'name' => Request::val('name', ''),
		'designation' => Request::val('designation', ''),
		'qualification' => Request::val('qualification', ''),
		'self_description' => br2nl(Request::val('self_description', '')),
		'role' => Request::val('role', ''),
		'last_updated_by' => parseCode('<%%editorUsername%%>', false),
		'last_updated_at' => parseCode('<%%editingDateTime%%>', false),
	];

	// get existing values
	$old_data = getRecord('evaluators_table', $selected_id);
	if(is_array($old_data)) {
		$old_data = array_map('makeSafe', $old_data);
		$old_data['selectedID'] = makeSafe($selected_id);
	}

	$data['selectedID'] = makeSafe($selected_id);

	// hook: evaluators_table_before_update
	if(function_exists('evaluators_table_before_update')) {
		$args = ['old_data' => $old_data];
		if(!evaluators_table_before_update($data, getMemberInfo(), $args)) {
			if(isset($args['error_message'])) $error_message = $args['error_message'];
			return false;
		}
	}

	$set = $data; unset($set['selectedID']);
	foreach ($set as $field => $value) {
		$set[$field] = ($value !== '' && $value !== NULL) ? $value : NULL;
	}

	if(!update(
		'evaluators_table',
		backtick_keys_once($set),
		['`evaluator_id`' => $selected_id],
		$error_message
	)) {
		echo $error_message;
		echo '<a href="evaluators_table_view.php?SelectedID=' . urlencode($selected_id) . "\">{$Translation['< back']}</a>";
		exit;
	}


	update_calc_fields('evaluators_table', $data['selectedID'], calculated_fields()['evaluators_table']);

	// hook: evaluators_table_after_update
	if(function_exists('evaluators_table_after_update')) {
		if($row = getRecord('evaluators_table', $data['selectedID'])) $data = array_map('makeSafe', $row);

		$data['selectedID'] = $data['evaluator_id'];
		$args = ['old_data' => $old_data];
		if(!evaluators_table_after_update($data, getMemberInfo(), $args)) return;
	}

	// mm: update record update timestamp
	set_record_owner('evaluators_table', $selected_id);
}

function evaluators_table_form($selectedId = '', $allowUpdate = true, $allowInsert = true, $allowDelete = true, $separateDV = true, $templateDV = '', $templateDVP = '') {
	// function to return an editable form for a table records
	// and fill it with data of record whose ID is $selectedId. If $selectedId
	// is empty, an empty form is shown, with only an 'Add New'
	// button displayed.

	global $Translation;
	$eo = ['silentErrors' => true];
	$noUploads = $row = $urow = $jsReadOnly = $jsEditable = $lookups = null;
	$noSaveAsCopy = true;
	$hasSelectedId = strlen($selectedId) > 0;

	// mm: get table permissions
	$arrPerm = getTablePermissions('evaluators_table');
	$allowInsert = ($arrPerm['insert'] ? true : false);
	$allowUpdate = $hasSelectedId && check_record_permission('evaluators_table', $selectedId, 'edit');
	$allowDelete = $hasSelectedId && check_record_permission('evaluators_table', $selectedId, 'delete');

	if(!$allowInsert && !$hasSelectedId)
		// no insert permission and no record selected
		// so show access denied error -- except if TVDV: just hide DV
		return $separateDV ? $Translation['tableAccessDenied'] : '';

	if($hasSelectedId && !check_record_permission('evaluators_table', $selectedId, 'view'))
		return $Translation['tableAccessDenied'];

	// print preview?
	$dvprint = $hasSelectedId && Request::val('dvprint_x') != '';

	$showSaveNew = !$dvprint && ($allowInsert && !$hasSelectedId);
	$showSaveChanges = !$dvprint && $allowUpdate && $hasSelectedId;
	$showDelete = !$dvprint && $allowDelete && $hasSelectedId;
	$showSaveAsCopy = !$dvprint && ($allowInsert && $hasSelectedId && !$noSaveAsCopy);
	$fieldsAreEditable = !$dvprint && (($allowInsert && !$hasSelectedId) || ($allowUpdate && $hasSelectedId) || $showSaveAsCopy);

	$filterer_evaluation_lookup = Request::val('filterer_evaluation_lookup');

	// populate filterers, starting from children to grand-parents

	// unique random identifier
	$rnd1 = ($dvprint ? rand(1000000, 9999999) : '');
	// combobox: evaluation_lookup
	$combo_evaluation_lookup = new DataCombo;
	// combobox: role
	$combo_role = new Combo;
	$combo_role->ListType = 0;
	$combo_role->MultipleSeparator = ', ';
	$combo_role->ListBoxHeight = 10;
	$combo_role->RadiosPerLine = 1;
	if(is_file(__DIR__ . '/hooks/evaluators_table.role.csv')) {
		$role_data = addslashes(implode('', @file(__DIR__ . '/hooks/evaluators_table.role.csv')));
		$combo_role->ListItem = array_trim(explode('||', entitiesToUTF8(convertLegacyOptions($role_data))));
		$combo_role->ListData = $combo_role->ListItem;
	} else {
		$combo_role->ListItem = array_trim(explode('||', entitiesToUTF8(convertLegacyOptions("Technical Mentor;;Financial Mentor;;Growth Mentor;;Buisiness Mentor;;Process Mentor"))));
		$combo_role->ListData = $combo_role->ListItem;
	}
	$combo_role->SelectName = 'role';

	if($hasSelectedId) {
		if(!($row = getRecord('evaluators_table', $selectedId))) {
			return error_message($Translation['No records found'], 'evaluators_table_view.php', false);
		}
		$combo_evaluation_lookup->SelectedData = $row['evaluation_lookup'];
		$combo_role->SelectedData = $row['role'];
		$urow = $row; /* unsanitized data */
		$row = array_map('safe_html', $row);
	} else {
		$filterField = Request::val('FilterField');
		$filterOperator = Request::val('FilterOperator');
		$filterValue = Request::val('FilterValue');
		$combo_evaluation_lookup->SelectedData = $filterer_evaluation_lookup;
		$combo_role->SelectedText = (isset($filterField[1]) && $filterField[1] == '7' && $filterOperator[1] == '<=>' ? $filterValue[1] : entitiesToUTF8(''));
	}
	$combo_evaluation_lookup->HTML = '<span id="evaluation_lookup-container' . $rnd1 . '"></span><input type="hidden" name="evaluation_lookup" id="evaluation_lookup' . $rnd1 . '" value="' . html_attr($combo_evaluation_lookup->SelectedData) . '">';
	$combo_evaluation_lookup->MatchText = '<span id="evaluation_lookup-container-readonly' . $rnd1 . '"></span><input type="hidden" name="evaluation_lookup" id="evaluation_lookup' . $rnd1 . '" value="' . html_attr($combo_evaluation_lookup->SelectedData) . '">';
	$combo_role->Render();

	ob_start();
	?>

	<script>
		// initial lookup values
		AppGini.current_evaluation_lookup__RAND__ = { text: "", value: "<?php echo addslashes($hasSelectedId ? $urow['evaluation_lookup'] : htmlspecialchars($filterer_evaluation_lookup, ENT_QUOTES)); ?>"};

		$j(function() {
			setTimeout(function() {
				if(typeof(evaluation_lookup_reload__RAND__) == 'function') evaluation_lookup_reload__RAND__();
			}, 50); /* we need to slightly delay client-side execution of the above code to allow AppGini.ajaxCache to work */
		});
		function evaluation_lookup_reload__RAND__() {
		<?php if($fieldsAreEditable) { ?>

			$j("#evaluation_lookup-container__RAND__").select2({
				/* initial default value */
				initSelection: function(e, c) {
					$j.ajax({
						url: 'ajax_combo.php',
						dataType: 'json',
						data: { id: AppGini.current_evaluation_lookup__RAND__.value, t: 'evaluators_table', f: 'evaluation_lookup' },
						success: function(resp) {
							c({
								id: resp.results[0].id,
								text: resp.results[0].text
							});
							$j('[name="evaluation_lookup"]').val(resp.results[0].id);
							$j('[id=evaluation_lookup-container-readonly__RAND__]').html('<span class="match-text" id="evaluation_lookup-match-text">' + resp.results[0].text + '</span>');
							if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=evaluation_table_view_parent]').hide(); } else { $j('.btn[id=evaluation_table_view_parent]').show(); }


							if(typeof(evaluation_lookup_update_autofills__RAND__) == 'function') evaluation_lookup_update_autofills__RAND__();
						}
					});
				},
				width: '100%',
				formatNoMatches: function(term) { return '<?php echo addslashes($Translation['No matches found!']); ?>'; },
				minimumResultsForSearch: 5,
				loadMorePadding: 200,
				ajax: {
					url: 'ajax_combo.php',
					dataType: 'json',
					cache: true,
					data: function(term, page) { return { s: term, p: page, t: 'evaluators_table', f: 'evaluation_lookup' }; },
					results: function(resp, page) { return resp; }
				},
				escapeMarkup: function(str) { return str; }
			}).on('change', function(e) {
				AppGini.current_evaluation_lookup__RAND__.value = e.added.id;
				AppGini.current_evaluation_lookup__RAND__.text = e.added.text;
				$j('[name="evaluation_lookup"]').val(e.added.id);
				$j(this).parents('.form-group')
					.find('.btn[id=evaluation_table_view_parent]')
					.toggleClass('hidden', e.added.id == '<?php echo empty_lookup_value; ?>');


				if(typeof(evaluation_lookup_update_autofills__RAND__) == 'function') evaluation_lookup_update_autofills__RAND__();
			});

			if(!$j("#evaluation_lookup-container__RAND__").length) {
				$j.ajax({
					url: 'ajax_combo.php',
					dataType: 'json',
					data: { id: AppGini.current_evaluation_lookup__RAND__.value, t: 'evaluators_table', f: 'evaluation_lookup' },
					success: function(resp) {
						$j('[name="evaluation_lookup"]').val(resp.results[0].id);
						$j('[id=evaluation_lookup-container-readonly__RAND__]').html('<span class="match-text" id="evaluation_lookup-match-text">' + resp.results[0].text + '</span>');
						if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=evaluation_table_view_parent]').hide(); } else { $j('.btn[id=evaluation_table_view_parent]').show(); }

						if(typeof(evaluation_lookup_update_autofills__RAND__) == 'function') evaluation_lookup_update_autofills__RAND__();
					}
				});
			}

		<?php } else { ?>

			$j.ajax({
				url: 'ajax_combo.php',
				dataType: 'json',
				data: { id: AppGini.current_evaluation_lookup__RAND__.value, t: 'evaluators_table', f: 'evaluation_lookup' },
				success: function(resp) {
					$j('[id=evaluation_lookup-container__RAND__], [id=evaluation_lookup-container-readonly__RAND__]').html('<span class="match-text" id="evaluation_lookup-match-text">' + resp.results[0].text + '</span>');
					if(resp.results[0].id == '<?php echo empty_lookup_value; ?>') { $j('.btn[id=evaluation_table_view_parent]').hide(); } else { $j('.btn[id=evaluation_table_view_parent]').show(); }

					if(typeof(evaluation_lookup_update_autofills__RAND__) == 'function') evaluation_lookup_update_autofills__RAND__();
				}
			});
		<?php } ?>

		}
	</script>
	<?php

	$lookups = str_replace('__RAND__', $rnd1, ob_get_clean());


	// code for template based detail view forms

	// open the detail view template
	if($dvprint) {
		$template_file = is_file("./{$templateDVP}") ? "./{$templateDVP}" : './templates/evaluators_table_templateDVP.html';
		$templateCode = @file_get_contents($template_file);
	} else {
		$template_file = is_file("./{$templateDV}") ? "./{$templateDV}" : './templates/evaluators_table_templateDV.html';
		$templateCode = @file_get_contents($template_file);
	}

	// process form title
	$templateCode = str_replace('<%%DETAIL_VIEW_TITLE%%>', 'Evaluators table details', $templateCode);
	$templateCode = str_replace('<%%RND1%%>', $rnd1, $templateCode);
	$templateCode = str_replace('<%%EMBEDDED%%>', (Request::val('Embedded') ? 'Embedded=1' : ''), $templateCode);
	// process buttons
	if($showSaveNew) {
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '<button type="submit" class="btn btn-success" id="insert" name="insert_x" value="1"><i class="glyphicon glyphicon-plus-sign"></i> ' . $Translation['Save New'] . '</button>', $templateCode);
	} elseif($showSaveAsCopy) {
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="insert" name="insert_x" value="1"><i class="glyphicon glyphicon-plus-sign"></i> ' . $Translation['Save As Copy'] . '</button>', $templateCode);
	} else {
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '', $templateCode);
	}

	// 'Back' button action
	if(Request::val('Embedded')) {
		$backAction = 'AppGini.closeParentModal(); return false;';
	} else {
		$backAction = 'return true;';
	}

	if($hasSelectedId) {
		if(!Request::val('Embedded')) $templateCode = str_replace('<%%DVPRINT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="dvprint" name="dvprint_x" value="1" title="' . html_attr($Translation['Print Preview']) . '"><i class="glyphicon glyphicon-print"></i> ' . $Translation['Print Preview'] . '</button>', $templateCode);
		if($allowUpdate)
			$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '<button type="submit" class="btn btn-success btn-lg" id="update" name="update_x" value="1" title="' . html_attr($Translation['Save Changes']) . '"><i class="glyphicon glyphicon-ok"></i> ' . $Translation['Save Changes'] . '</button>', $templateCode);
		else
			$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '', $templateCode);

		if($allowDelete)
			$templateCode = str_replace('<%%DELETE_BUTTON%%>', '<button type="submit" class="btn btn-danger" id="delete" name="delete_x" value="1" title="' . html_attr($Translation['Delete']) . '"><i class="glyphicon glyphicon-trash"></i> ' . $Translation['Delete'] . '</button>', $templateCode);
		else
			$templateCode = str_replace('<%%DELETE_BUTTON%%>', '', $templateCode);

		$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '<button type="submit" class="btn btn-default ltr" id="deselect" name="deselect_x" value="1" onclick="' . $backAction . '" title="' . html_attr($Translation['Back']) . '"><i class="glyphicon glyphicon-chevron-left"></i> ' . $Translation['Back'] . '</button>', $templateCode);
	} else {
		$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '', $templateCode);
		$templateCode = str_replace('<%%DELETE_BUTTON%%>', '', $templateCode);

		// if not in embedded mode and user has insert only but no view/update/delete,
		// remove 'back' button
		if(
			$allowInsert
			&& !$allowUpdate && !$allowDelete && !$arrPerm['view']
			&& !Request::val('Embedded')
		)
			$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '', $templateCode);
		elseif($separateDV)
			$templateCode = str_replace(
				'<%%DESELECT_BUTTON%%>',
				'<button
					type="submit"
					class="btn btn-default ltr"
					id="deselect"
					name="deselect_x"
					value="1"
					onclick="' . $backAction . '"
					title="' . html_attr($Translation['Back']) . '">
						<i class="glyphicon glyphicon-chevron-left"></i> ' .
						$Translation['Back'] .
				'</button>',
				$templateCode
			);
		else
			$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '', $templateCode);
	}

	// set records to read only if user can't insert new records and can't edit current record
	if(!$fieldsAreEditable) {
		$jsReadOnly = '';
		$jsReadOnly .= "\t\$j('#name').replaceWith('<div class=\"form-control-static\" id=\"name\">' + (\$j('#name').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('#designation').replaceWith('<div class=\"form-control-static\" id=\"designation\">' + (\$j('#designation').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('#qualification').replaceWith('<div class=\"form-control-static\" id=\"qualification\">' + (\$j('#qualification').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('#self_description').replaceWith('<div class=\"form-control-static\" id=\"self_description\">' + (\$j('#self_description').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('#role').replaceWith('<div class=\"form-control-static\" id=\"role\">' + (\$j('#role').val() || '') + '</div>'); \$j('#role-multi-selection-help').hide();\n";
		$jsReadOnly .= "\t\$j('.select2-container').hide();\n";

		$noUploads = true;
	} else {
		// temporarily disable form change handler till time and datetime pickers are enabled
		$jsEditable = "\t\$j('form').eq(0).data('already_changed', true);";
		$jsEditable .= "\t\$j('form').eq(0).data('already_changed', false);"; // re-enable form change handler
	}

	// process combos
	$templateCode = str_replace('<%%COMBO(evaluation_lookup)%%>', $combo_evaluation_lookup->HTML, $templateCode);
	$templateCode = str_replace('<%%COMBOTEXT(evaluation_lookup)%%>', $combo_evaluation_lookup->MatchText, $templateCode);
	$templateCode = str_replace('<%%URLCOMBOTEXT(evaluation_lookup)%%>', urlencode($combo_evaluation_lookup->MatchText), $templateCode);
	$templateCode = str_replace('<%%COMBO(role)%%>', $combo_role->HTML, $templateCode);
	$templateCode = str_replace('<%%COMBOTEXT(role)%%>', $combo_role->SelectedData, $templateCode);

	/* lookup fields array: 'lookup field name' => ['parent table name', 'lookup field caption'] */
	$lookup_fields = ['evaluation_lookup' => ['evaluation_table', 'Evaluation lookup'], ];
	foreach($lookup_fields as $luf => $ptfc) {
		$pt_perm = getTablePermissions($ptfc[0]);

		// process foreign key links
		if(($pt_perm['view'] && isDetailViewEnabled($ptfc[0])) || $pt_perm['edit']) {
			$templateCode = str_replace("<%%PLINK({$luf})%%>", '<button type="button" class="btn btn-default view_parent" id="' . $ptfc[0] . '_view_parent" title="' . html_attr($Translation['View'] . ' ' . $ptfc[1]) . '"><i class="glyphicon glyphicon-eye-open"></i></button>', $templateCode);
		}

		// if user has insert permission to parent table of a lookup field, put an add new button
		if($pt_perm['insert'] /* && !Request::val('Embedded')*/) {
			$templateCode = str_replace("<%%ADDNEW({$ptfc[0]})%%>", '<button type="button" class="btn btn-default add_new_parent" id="' . $ptfc[0] . '_add_new" title="' . html_attr($Translation['Add New'] . ' ' . $ptfc[1]) . '"><i class="glyphicon glyphicon-plus text-success"></i></button>', $templateCode);
		}
	}

	// process images
	$templateCode = str_replace('<%%UPLOADFILE(evaluator_id)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(evaluation_lookup)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(name)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(designation)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(qualification)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(self_description)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(role)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(created_by)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(created_at)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(last_updated_by)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(last_updated_at)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(created_by_username)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(last_updated_by_username)%%>', '', $templateCode);

	// process values
	if($hasSelectedId) {
		$templateCode = str_replace('<%%VALUE(evaluator_id)%%>', safe_html($urow['evaluator_id']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(evaluator_id)%%>', urlencode($urow['evaluator_id']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(evaluation_lookup)%%>', safe_html($urow['evaluation_lookup']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(evaluation_lookup)%%>', html_attr($row['evaluation_lookup']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(evaluation_lookup)%%>', urlencode($urow['evaluation_lookup']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(name)%%>', safe_html($urow['name']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(name)%%>', html_attr($row['name']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(name)%%>', urlencode($urow['name']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(designation)%%>', safe_html($urow['designation']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(designation)%%>', html_attr($row['designation']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(designation)%%>', urlencode($urow['designation']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(qualification)%%>', safe_html($urow['qualification']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(qualification)%%>', html_attr($row['qualification']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(qualification)%%>', urlencode($urow['qualification']), $templateCode);
		$templateCode = str_replace('<%%VALUE(self_description)%%>', safe_html($urow['self_description'], $fieldsAreEditable), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(self_description)%%>', urlencode($urow['self_description']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(role)%%>', safe_html($urow['role']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(role)%%>', html_attr($row['role']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(role)%%>', urlencode($urow['role']), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_by)%%>', safe_html($urow['created_by']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_by)%%>', urlencode($urow['created_by']), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_at)%%>', safe_html($urow['created_at']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_at)%%>', urlencode($urow['created_at']), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_by)%%>', safe_html($urow['last_updated_by']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_by)%%>', urlencode($urow['last_updated_by']), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_at)%%>', safe_html($urow['last_updated_at']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_at)%%>', urlencode($urow['last_updated_at']), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_by_username)%%>', safe_html($urow['created_by_username']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_by_username)%%>', urlencode($urow['created_by_username']), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_by_username)%%>', safe_html($urow['last_updated_by_username']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_by_username)%%>', urlencode($urow['last_updated_by_username']), $templateCode);
	} else {
		$templateCode = str_replace('<%%VALUE(evaluator_id)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(evaluator_id)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(evaluation_lookup)%%>', ( Request::val('FilterField')[1]=='2' && Request::val('FilterOperator')[1]=='<=>' ? $combo_evaluation_lookup->SelectedData : ''), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(evaluation_lookup)%%>', urlencode( Request::val('FilterField')[1]=='2' && Request::val('FilterOperator')[1]=='<=>' ? $combo_evaluation_lookup->SelectedData : ''), $templateCode);
		$templateCode = str_replace('<%%VALUE(name)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(name)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(designation)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(designation)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(qualification)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(qualification)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(self_description)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(self_description)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(role)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(role)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_by)%%>', '<%%creatorUsername%%>', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_by)%%>', urlencode('<%%creatorUsername%%>'), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_at)%%>', '<%%creationDateTime%%>', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_at)%%>', urlencode('<%%creationDateTime%%>'), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_by)%%>', '<%%editorUsername%%>', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_by)%%>', urlencode('<%%editorUsername%%>'), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_at)%%>', '<%%editingDateTime%%>', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_at)%%>', urlencode('<%%editingDateTime%%>'), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_by_username)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_by_username)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_by_username)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_by_username)%%>', urlencode(''), $templateCode);
	}

	// process translations
	$templateCode = parseTemplate($templateCode);

	// clear scrap
	$templateCode = str_replace('<%%', '<!-- ', $templateCode);
	$templateCode = str_replace('%%>', ' -->', $templateCode);

	// hide links to inaccessible tables
	if(Request::val('dvprint_x') == '') {
		$templateCode .= "\n\n<script>\$j(function() {\n";
		$arrTables = getTableList();
		foreach($arrTables as $name => $caption) {
			$templateCode .= "\t\$j('#{$name}_link').removeClass('hidden');\n";
			$templateCode .= "\t\$j('#xs_{$name}_link').removeClass('hidden');\n";
		}

		$templateCode .= $jsReadOnly;
		$templateCode .= $jsEditable;

		if(!$hasSelectedId) {
		}

		$templateCode.="\n});</script>\n";
	}

	// ajaxed auto-fill fields
	$templateCode .= '<script>';
	$templateCode .= '$j(function() {';


	$templateCode.="});";
	$templateCode.="</script>";
	$templateCode .= $lookups;

	// handle enforced parent values for read-only lookup fields
	$filterField = Request::val('FilterField');
	$filterOperator = Request::val('FilterOperator');
	$filterValue = Request::val('FilterValue');

	// don't include blank images in lightbox gallery
	$templateCode = preg_replace('/blank.gif" data-lightbox=".*?"/', 'blank.gif"', $templateCode);

	// don't display empty email links
	$templateCode=preg_replace('/<a .*?href="mailto:".*?<\/a>/', '', $templateCode);

	/* default field values */
	$rdata = $jdata = get_defaults('evaluators_table');
	if($hasSelectedId) {
		$jdata = get_joined_record('evaluators_table', $selectedId);
		if($jdata === false) $jdata = get_defaults('evaluators_table');
		$rdata = $row;
	}
	$templateCode .= loadView('evaluators_table-ajax-cache', ['rdata' => $rdata, 'jdata' => $jdata]);

	// hook: evaluators_table_dv
	if(function_exists('evaluators_table_dv')) {
		$args = [];
		evaluators_table_dv(($hasSelectedId ? $selectedId : FALSE), getMemberInfo(), $templateCode, $args);
	}

	return $templateCode;
}