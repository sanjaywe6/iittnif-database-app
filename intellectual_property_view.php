<?php
// This script and data application was generated by AppGini, https://bigprof.com/appgini
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/intellectual_property.php');
	include_once(__DIR__ . '/intellectual_property_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('intellectual_property');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'intellectual_property';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`intellectual_property`.`id`" => "id",
		"`intellectual_property`.`year`" => "year",
		"`intellectual_property`.`ip_category`" => "ip_category",
		"`intellectual_property`.`ip_title`" => "ip_title",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`intellectual_property`.`id`',
		2 => 2,
		3 => 3,
		4 => 4,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`intellectual_property`.`id`" => "id",
		"`intellectual_property`.`year`" => "year",
		"`intellectual_property`.`ip_category`" => "ip_category",
		"`intellectual_property`.`ip_title`" => "ip_title",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`intellectual_property`.`id`" => "ID",
		"`intellectual_property`.`year`" => "Year",
		"`intellectual_property`.`ip_category`" => "IP Category",
		"`intellectual_property`.`ip_title`" => "IP Title",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`intellectual_property`.`id`" => "id",
		"`intellectual_property`.`year`" => "year",
		"`intellectual_property`.`ip_category`" => "ip_category",
		"`intellectual_property`.`ip_title`" => "ip_title",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`intellectual_property` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->AllowAdminShowSQL = showSQL();
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'intellectual_property_view.php';
	$x->TableTitle = 'Intellectual Property';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`intellectual_property`.`id`';
	$x->DefaultSortField = '1';
	$x->DefaultSortDirection = 'desc';

	$x->ColWidth = [150, 150, 150, ];
	$x->ColCaption = ['Year', 'IP Category', 'IP Title', ];
	$x->ColFieldName = ['year', 'ip_category', 'ip_title', ];
	$x->ColNumber  = [2, 3, 4, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/intellectual_property_templateTV.html';
	$x->SelectedTemplate = 'templates/intellectual_property_templateTVS.html';
	$x->TemplateDV = 'templates/intellectual_property_templateDV.html';
	$x->TemplateDVP = 'templates/intellectual_property_templateDVP.html';

	$x->ShowTableHeader = 0;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: intellectual_property_init
	$render = true;
	if(function_exists('intellectual_property_init')) {
		$args = [];
		$render = intellectual_property_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: intellectual_property_header
	$headerCode = '';
	if(function_exists('intellectual_property_header')) {
		$args = [];
		$headerCode = intellectual_property_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php');
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: intellectual_property_footer
	$footerCode = '';
	if(function_exists('intellectual_property_footer')) {
		$args = [];
		$footerCode = intellectual_property_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php');
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
