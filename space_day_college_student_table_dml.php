<?php

// Data functions (insert, update, delete, form) for table space_day_college_student_table

// This script and data application was generated by AppGini, https://bigprof.com/appgini
// Download AppGini for free from https://bigprof.com/appgini/download/

function space_day_college_student_table_insert(&$error_message = '') {
	global $Translation;

	// mm: can member insert record?
	$arrPerm = getTablePermissions('space_day_college_student_table');
	if(!$arrPerm['insert']) {
		$error_message = $Translation['no insert permission'];
		return false;
	}

	$data = [
		'username' => parseCode('<%%creatorUsername%%>', true),
		'name_of_student' => Request::val('name_of_student', ''),
		'registration_number' => Request::val('registration_number', ''),
		'degree_department' => Request::val('degree_department', ''),
		'gender' => Request::val('gender', ''),
		'home_address' => Request::val('home_address', ''),
		'email_id' => Request::val('email_id', ''),
		'contact_number' => Request::val('contact_number', ''),
		'interest' => Request::val('interest', ''),
		'college_name' => Request::val('college_name', ''),
		'created_by' => parseCode('<%%creatorUsername%%>', true),
		'created_at' => parseCode('<%%creationDateTime%%>', true),
	];

	// record owner is current user
	$recordOwner = getLoggedMemberID();

	$recID = tableInsert('space_day_college_student_table', $data, $recordOwner, $error_message);

	// if this record is a copy of another record, copy children if applicable
	if(strlen(Request::val('SelectedID')) && $recID !== false)
		space_day_college_student_table_copy_children($recID, Request::val('SelectedID'));

	return $recID;
}

function space_day_college_student_table_copy_children($destination_id, $source_id) {
	global $Translation;
	$requests = []; // array of curl handlers for launching insert requests
	$eo = ['silentErrors' => true];
	$safe_sid = makeSafe($source_id);
	$currentUsername = getLoggedMemberID();
	$errorMessage = '';

	// launch requests, asynchronously
	curl_batch($requests);
}

function space_day_college_student_table_delete($selected_id, $AllowDeleteOfParents = false, $skipChecks = false) {
	// insure referential integrity ...
	global $Translation;
	$selected_id = makeSafe($selected_id);

	// mm: can member delete record?
	if(!check_record_permission('space_day_college_student_table', $selected_id, 'delete')) {
		return $Translation['You don\'t have enough permissions to delete this record'];
	}

	// hook: space_day_college_student_table_before_delete
	if(function_exists('space_day_college_student_table_before_delete')) {
		$args = [];
		if(!space_day_college_student_table_before_delete($selected_id, $skipChecks, getMemberInfo(), $args))
			return $Translation['Couldn\'t delete this record'] . (
				!empty($args['error_message']) ?
					'<div class="text-bold">' . strip_tags($args['error_message']) . '</div>'
					: ''
			);
	}

	sql("DELETE FROM `space_day_college_student_table` WHERE `id`='{$selected_id}'", $eo);

	// hook: space_day_college_student_table_after_delete
	if(function_exists('space_day_college_student_table_after_delete')) {
		$args = [];
		space_day_college_student_table_after_delete($selected_id, getMemberInfo(), $args);
	}

	// mm: delete ownership data
	sql("DELETE FROM `membership_userrecords` WHERE `tableName`='space_day_college_student_table' AND `pkValue`='{$selected_id}'", $eo);
}

function space_day_college_student_table_update(&$selected_id, &$error_message = '') {
	global $Translation;

	// mm: can member edit record?
	if(!check_record_permission('space_day_college_student_table', $selected_id, 'edit')) return false;

	$data = [
		'name_of_student' => Request::val('name_of_student', ''),
		'registration_number' => Request::val('registration_number', ''),
		'degree_department' => Request::val('degree_department', ''),
		'gender' => Request::val('gender', ''),
		'home_address' => Request::val('home_address', ''),
		'email_id' => Request::val('email_id', ''),
		'contact_number' => Request::val('contact_number', ''),
		'interest' => Request::val('interest', ''),
		'college_name' => Request::val('college_name', ''),
		'last_updated_by' => parseCode('<%%editorUsername%%>', false),
		'last_updated_at' => parseCode('<%%editingDateTime%%>', false),
	];

	// get existing values
	$old_data = getRecord('space_day_college_student_table', $selected_id);
	if(is_array($old_data)) {
		$old_data = array_map('makeSafe', $old_data);
		$old_data['selectedID'] = makeSafe($selected_id);
	}

	$data['selectedID'] = makeSafe($selected_id);

	// hook: space_day_college_student_table_before_update
	if(function_exists('space_day_college_student_table_before_update')) {
		$args = ['old_data' => $old_data];
		if(!space_day_college_student_table_before_update($data, getMemberInfo(), $args)) {
			if(isset($args['error_message'])) $error_message = $args['error_message'];
			return false;
		}
	}

	$set = $data; unset($set['selectedID']);
	foreach ($set as $field => $value) {
		$set[$field] = ($value !== '' && $value !== NULL) ? $value : NULL;
	}

	if(!update(
		'space_day_college_student_table',
		backtick_keys_once($set),
		['`id`' => $selected_id],
		$error_message
	)) {
		echo $error_message;
		echo '<a href="space_day_college_student_table_view.php?SelectedID=' . urlencode($selected_id) . "\">{$Translation['< back']}</a>";
		exit;
	}


	update_calc_fields('space_day_college_student_table', $data['selectedID'], calculated_fields()['space_day_college_student_table']);

	// hook: space_day_college_student_table_after_update
	if(function_exists('space_day_college_student_table_after_update')) {
		if($row = getRecord('space_day_college_student_table', $data['selectedID'])) $data = array_map('makeSafe', $row);

		$data['selectedID'] = $data['id'];
		$args = ['old_data' => $old_data];
		if(!space_day_college_student_table_after_update($data, getMemberInfo(), $args)) return;
	}

	// mm: update record update timestamp
	set_record_owner('space_day_college_student_table', $selected_id);
}

function space_day_college_student_table_form($selectedId = '', $allowUpdate = true, $allowInsert = true, $allowDelete = true, $separateDV = true, $templateDV = '', $templateDVP = '') {
	// function to return an editable form for a table records
	// and fill it with data of record whose ID is $selectedId. If $selectedId
	// is empty, an empty form is shown, with only an 'Add New'
	// button displayed.

	global $Translation;
	$eo = ['silentErrors' => true];
	$noUploads = $row = $urow = $jsReadOnly = $jsEditable = $lookups = null;
	$noSaveAsCopy = true;
	$hasSelectedId = strlen($selectedId) > 0;

	// mm: get table permissions
	$arrPerm = getTablePermissions('space_day_college_student_table');
	$allowInsert = ($arrPerm['insert'] ? true : false);
	$allowUpdate = $hasSelectedId && check_record_permission('space_day_college_student_table', $selectedId, 'edit');
	$allowDelete = $hasSelectedId && check_record_permission('space_day_college_student_table', $selectedId, 'delete');

	if(!$allowInsert && !$hasSelectedId)
		// no insert permission and no record selected
		// so show access denied error -- except if TVDV: just hide DV
		return $separateDV ? $Translation['tableAccessDenied'] : '';

	if($hasSelectedId && !check_record_permission('space_day_college_student_table', $selectedId, 'view'))
		return $Translation['tableAccessDenied'];

	// print preview?
	$dvprint = $hasSelectedId && Request::val('dvprint_x') != '';

	$showSaveNew = !$dvprint && ($allowInsert && !$hasSelectedId);
	$showSaveChanges = !$dvprint && $allowUpdate && $hasSelectedId;
	$showDelete = !$dvprint && $allowDelete && $hasSelectedId;
	$showSaveAsCopy = !$dvprint && ($allowInsert && $hasSelectedId && !$noSaveAsCopy);
	$fieldsAreEditable = !$dvprint && (($allowInsert && !$hasSelectedId) || ($allowUpdate && $hasSelectedId) || $showSaveAsCopy);


	// populate filterers, starting from children to grand-parents

	// unique random identifier
	$rnd1 = ($dvprint ? rand(1000000, 9999999) : '');
	// combobox: gender
	$combo_gender = new Combo;
	$combo_gender->ListType = 0;
	$combo_gender->MultipleSeparator = ', ';
	$combo_gender->ListBoxHeight = 10;
	$combo_gender->RadiosPerLine = 1;
	if(is_file(__DIR__ . '/hooks/space_day_college_student_table.gender.csv')) {
		$gender_data = addslashes(implode('', @file(__DIR__ . '/hooks/space_day_college_student_table.gender.csv')));
		$combo_gender->ListItem = array_trim(explode('||', entitiesToUTF8(convertLegacyOptions($gender_data))));
		$combo_gender->ListData = $combo_gender->ListItem;
	} else {
		$combo_gender->ListItem = array_trim(explode('||', entitiesToUTF8(convertLegacyOptions("Male;;Female;;Prefer not to say"))));
		$combo_gender->ListData = $combo_gender->ListItem;
	}
	$combo_gender->SelectName = 'gender';

	if($hasSelectedId) {
		if(!($row = getRecord('space_day_college_student_table', $selectedId))) {
			return error_message($Translation['No records found'], 'space_day_college_student_table_view.php', false);
		}
		$combo_gender->SelectedData = $row['gender'];
		$urow = $row; /* unsanitized data */
		$row = array_map('safe_html', $row);
	} else {
		$filterField = Request::val('FilterField');
		$filterOperator = Request::val('FilterOperator');
		$filterValue = Request::val('FilterValue');
		$combo_gender->SelectedText = (isset($filterField[1]) && $filterField[1] == '6' && $filterOperator[1] == '<=>' ? $filterValue[1] : entitiesToUTF8(''));
	}
	$combo_gender->Render();

	ob_start();
	?>

	<script>
		// initial lookup values

		$j(function() {
			setTimeout(function() {
			}, 50); /* we need to slightly delay client-side execution of the above code to allow AppGini.ajaxCache to work */
		});
	</script>
	<?php

	$lookups = str_replace('__RAND__', $rnd1, ob_get_clean());


	// code for template based detail view forms

	// open the detail view template
	if($dvprint) {
		$template_file = is_file("./{$templateDVP}") ? "./{$templateDVP}" : './templates/space_day_college_student_table_templateDVP.html';
		$templateCode = @file_get_contents($template_file);
	} else {
		$template_file = is_file("./{$templateDV}") ? "./{$templateDV}" : './templates/space_day_college_student_table_templateDV.html';
		$templateCode = @file_get_contents($template_file);
	}

	// process form title
	$templateCode = str_replace('<%%DETAIL_VIEW_TITLE%%>', 'Space day college student table details', $templateCode);
	$templateCode = str_replace('<%%RND1%%>', $rnd1, $templateCode);
	$templateCode = str_replace('<%%EMBEDDED%%>', (Request::val('Embedded') ? 'Embedded=1' : ''), $templateCode);
	// process buttons
	if($showSaveNew) {
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '<button type="submit" class="btn btn-success" id="insert" name="insert_x" value="1"><i class="glyphicon glyphicon-plus-sign"></i> ' . $Translation['Save New'] . '</button>', $templateCode);
	} elseif($showSaveAsCopy) {
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="insert" name="insert_x" value="1"><i class="glyphicon glyphicon-plus-sign"></i> ' . $Translation['Save As Copy'] . '</button>', $templateCode);
	} else {
		$templateCode = str_replace('<%%INSERT_BUTTON%%>', '', $templateCode);
	}

	// 'Back' button action
	if(Request::val('Embedded')) {
		$backAction = 'AppGini.closeParentModal(); return false;';
	} else {
		$backAction = 'return true;';
	}

	if($hasSelectedId) {
		if(!Request::val('Embedded')) $templateCode = str_replace('<%%DVPRINT_BUTTON%%>', '<button type="submit" class="btn btn-default" id="dvprint" name="dvprint_x" value="1" title="' . html_attr($Translation['Print Preview']) . '"><i class="glyphicon glyphicon-print"></i> ' . $Translation['Print Preview'] . '</button>', $templateCode);
		if($allowUpdate)
			$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '<button type="submit" class="btn btn-success btn-lg" id="update" name="update_x" value="1" title="' . html_attr($Translation['Save Changes']) . '"><i class="glyphicon glyphicon-ok"></i> ' . $Translation['Save Changes'] . '</button>', $templateCode);
		else
			$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '', $templateCode);

		if($allowDelete)
			$templateCode = str_replace('<%%DELETE_BUTTON%%>', '<button type="submit" class="btn btn-danger" id="delete" name="delete_x" value="1" title="' . html_attr($Translation['Delete']) . '"><i class="glyphicon glyphicon-trash"></i> ' . $Translation['Delete'] . '</button>', $templateCode);
		else
			$templateCode = str_replace('<%%DELETE_BUTTON%%>', '', $templateCode);

		$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '<button type="submit" class="btn btn-default ltr" id="deselect" name="deselect_x" value="1" onclick="' . $backAction . '" title="' . html_attr($Translation['Back']) . '"><i class="glyphicon glyphicon-chevron-left"></i> ' . $Translation['Back'] . '</button>', $templateCode);
	} else {
		$templateCode = str_replace('<%%UPDATE_BUTTON%%>', '', $templateCode);
		$templateCode = str_replace('<%%DELETE_BUTTON%%>', '', $templateCode);

		// if not in embedded mode and user has insert only but no view/update/delete,
		// remove 'back' button
		if(
			$allowInsert
			&& !$allowUpdate && !$allowDelete && !$arrPerm['view']
			&& !Request::val('Embedded')
		)
			$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '', $templateCode);
		elseif($separateDV)
			$templateCode = str_replace(
				'<%%DESELECT_BUTTON%%>',
				'<button
					type="submit"
					class="btn btn-default ltr"
					id="deselect"
					name="deselect_x"
					value="1"
					onclick="' . $backAction . '"
					title="' . html_attr($Translation['Back']) . '">
						<i class="glyphicon glyphicon-chevron-left"></i> ' .
						$Translation['Back'] .
				'</button>',
				$templateCode
			);
		else
			$templateCode = str_replace('<%%DESELECT_BUTTON%%>', '', $templateCode);
	}

	// set records to read only if user can't insert new records and can't edit current record
	if(!$fieldsAreEditable) {
		$jsReadOnly = '';
		$jsReadOnly .= "\t\$j('#name_of_student').replaceWith('<div class=\"form-control-static\" id=\"name_of_student\">' + (\$j('#name_of_student').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('#registration_number').replaceWith('<div class=\"form-control-static\" id=\"registration_number\">' + (\$j('#registration_number').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('#degree_department').replaceWith('<div class=\"form-control-static\" id=\"degree_department\">' + (\$j('#degree_department').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('#gender').replaceWith('<div class=\"form-control-static\" id=\"gender\">' + (\$j('#gender').val() || '') + '</div>'); \$j('#gender-multi-selection-help').hide();\n";
		$jsReadOnly .= "\t\$j('#home_address').replaceWith('<div class=\"form-control-static\" id=\"home_address\">' + (\$j('#home_address').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('#email_id').replaceWith('<div class=\"form-control-static\" id=\"email_id\">' + (\$j('#email_id').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('#contact_number').replaceWith('<div class=\"form-control-static\" id=\"contact_number\">' + (\$j('#contact_number').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('#interest').replaceWith('<div class=\"form-control-static\" id=\"interest\">' + (\$j('#interest').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('#college_name').replaceWith('<div class=\"form-control-static\" id=\"college_name\">' + (\$j('#college_name').val() || '') + '</div>');\n";
		$jsReadOnly .= "\t\$j('.select2-container').hide();\n";

		$noUploads = true;
	} else {
		// temporarily disable form change handler till time and datetime pickers are enabled
		$jsEditable = "\t\$j('form').eq(0).data('already_changed', true);";
		$jsEditable .= "\t\$j('form').eq(0).data('already_changed', false);"; // re-enable form change handler
	}

	// process combos
	$templateCode = str_replace('<%%COMBO(gender)%%>', $combo_gender->HTML, $templateCode);
	$templateCode = str_replace('<%%COMBOTEXT(gender)%%>', $combo_gender->SelectedData, $templateCode);

	/* lookup fields array: 'lookup field name' => ['parent table name', 'lookup field caption'] */
	$lookup_fields = [];
	foreach($lookup_fields as $luf => $ptfc) {
		$pt_perm = getTablePermissions($ptfc[0]);

		// process foreign key links
		if(($pt_perm['view'] && isDetailViewEnabled($ptfc[0])) || $pt_perm['edit']) {
			$templateCode = str_replace("<%%PLINK({$luf})%%>", '<button type="button" class="btn btn-default view_parent" id="' . $ptfc[0] . '_view_parent" title="' . html_attr($Translation['View'] . ' ' . $ptfc[1]) . '"><i class="glyphicon glyphicon-eye-open"></i></button>', $templateCode);
		}

		// if user has insert permission to parent table of a lookup field, put an add new button
		if($pt_perm['insert'] /* && !Request::val('Embedded')*/) {
			$templateCode = str_replace("<%%ADDNEW({$ptfc[0]})%%>", '<button type="button" class="btn btn-default add_new_parent" id="' . $ptfc[0] . '_add_new" title="' . html_attr($Translation['Add New'] . ' ' . $ptfc[1]) . '"><i class="glyphicon glyphicon-plus text-success"></i></button>', $templateCode);
		}
	}

	// process images
	$templateCode = str_replace('<%%UPLOADFILE(id)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(username)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(name_of_student)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(registration_number)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(degree_department)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(gender)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(home_address)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(email_id)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(contact_number)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(interest)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(college_name)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(created_by)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(last_updated_by)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(created_by_username)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(created_at)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(last_updated_by_username)%%>', '', $templateCode);
	$templateCode = str_replace('<%%UPLOADFILE(last_updated_at)%%>', '', $templateCode);

	// process values
	if($hasSelectedId) {
		$templateCode = str_replace('<%%VALUE(id)%%>', safe_html($urow['id']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(id)%%>', urlencode($urow['id']), $templateCode);
		$templateCode = str_replace('<%%VALUE(username)%%>', safe_html($urow['username']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(username)%%>', urlencode($urow['username']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(name_of_student)%%>', safe_html($urow['name_of_student']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(name_of_student)%%>', html_attr($row['name_of_student']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(name_of_student)%%>', urlencode($urow['name_of_student']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(registration_number)%%>', safe_html($urow['registration_number']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(registration_number)%%>', html_attr($row['registration_number']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(registration_number)%%>', urlencode($urow['registration_number']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(degree_department)%%>', safe_html($urow['degree_department']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(degree_department)%%>', html_attr($row['degree_department']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(degree_department)%%>', urlencode($urow['degree_department']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(gender)%%>', safe_html($urow['gender']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(gender)%%>', html_attr($row['gender']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(gender)%%>', urlencode($urow['gender']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(home_address)%%>', safe_html($urow['home_address']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(home_address)%%>', html_attr($row['home_address']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(home_address)%%>', urlencode($urow['home_address']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(email_id)%%>', safe_html($urow['email_id']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(email_id)%%>', html_attr($row['email_id']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(email_id)%%>', urlencode($urow['email_id']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(contact_number)%%>', safe_html($urow['contact_number']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(contact_number)%%>', html_attr($row['contact_number']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(contact_number)%%>', urlencode($urow['contact_number']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(interest)%%>', safe_html($urow['interest']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(interest)%%>', html_attr($row['interest']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(interest)%%>', urlencode($urow['interest']), $templateCode);
		if( $dvprint) $templateCode = str_replace('<%%VALUE(college_name)%%>', safe_html($urow['college_name']), $templateCode);
		if(!$dvprint) $templateCode = str_replace('<%%VALUE(college_name)%%>', html_attr($row['college_name']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(college_name)%%>', urlencode($urow['college_name']), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_by)%%>', safe_html($urow['created_by']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_by)%%>', urlencode($urow['created_by']), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_by)%%>', safe_html($urow['last_updated_by']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_by)%%>', urlencode($urow['last_updated_by']), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_by_username)%%>', safe_html($urow['created_by_username']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_by_username)%%>', urlencode($urow['created_by_username']), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_at)%%>', safe_html($urow['created_at']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_at)%%>', urlencode($urow['created_at']), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_by_username)%%>', safe_html($urow['last_updated_by_username']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_by_username)%%>', urlencode($urow['last_updated_by_username']), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_at)%%>', safe_html($urow['last_updated_at']), $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_at)%%>', urlencode($urow['last_updated_at']), $templateCode);
	} else {
		$templateCode = str_replace('<%%VALUE(id)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(id)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(username)%%>', '<%%creatorUsername%%>', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(username)%%>', urlencode('<%%creatorUsername%%>'), $templateCode);
		$templateCode = str_replace('<%%VALUE(name_of_student)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(name_of_student)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(registration_number)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(registration_number)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(degree_department)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(degree_department)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(gender)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(gender)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(home_address)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(home_address)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(email_id)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(email_id)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(contact_number)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(contact_number)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(interest)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(interest)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(college_name)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(college_name)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_by)%%>', '<%%creatorUsername%%>', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_by)%%>', urlencode('<%%creatorUsername%%>'), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_by)%%>', '<%%editorUsername%%>', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_by)%%>', urlencode('<%%editorUsername%%>'), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_by_username)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_by_username)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(created_at)%%>', '<%%creationDateTime%%>', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(created_at)%%>', urlencode('<%%creationDateTime%%>'), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_by_username)%%>', '', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_by_username)%%>', urlencode(''), $templateCode);
		$templateCode = str_replace('<%%VALUE(last_updated_at)%%>', '<%%editingDateTime%%>', $templateCode);
		$templateCode = str_replace('<%%URLVALUE(last_updated_at)%%>', urlencode('<%%editingDateTime%%>'), $templateCode);
	}

	// process translations
	$templateCode = parseTemplate($templateCode);

	// clear scrap
	$templateCode = str_replace('<%%', '<!-- ', $templateCode);
	$templateCode = str_replace('%%>', ' -->', $templateCode);

	// hide links to inaccessible tables
	if(Request::val('dvprint_x') == '') {
		$templateCode .= "\n\n<script>\$j(function() {\n";
		$arrTables = getTableList();
		foreach($arrTables as $name => $caption) {
			$templateCode .= "\t\$j('#{$name}_link').removeClass('hidden');\n";
			$templateCode .= "\t\$j('#xs_{$name}_link').removeClass('hidden');\n";
		}

		$templateCode .= $jsReadOnly;
		$templateCode .= $jsEditable;

		if(!$hasSelectedId) {
		}

		$templateCode.="\n});</script>\n";
	}

	// ajaxed auto-fill fields
	$templateCode .= '<script>';
	$templateCode .= '$j(function() {';


	$templateCode.="});";
	$templateCode.="</script>";
	$templateCode .= $lookups;

	// handle enforced parent values for read-only lookup fields
	$filterField = Request::val('FilterField');
	$filterOperator = Request::val('FilterOperator');
	$filterValue = Request::val('FilterValue');

	// don't include blank images in lightbox gallery
	$templateCode = preg_replace('/blank.gif" data-lightbox=".*?"/', 'blank.gif"', $templateCode);

	// don't display empty email links
	$templateCode=preg_replace('/<a .*?href="mailto:".*?<\/a>/', '', $templateCode);

	/* default field values */
	$rdata = $jdata = get_defaults('space_day_college_student_table');
	if($hasSelectedId) {
		$jdata = get_joined_record('space_day_college_student_table', $selectedId);
		if($jdata === false) $jdata = get_defaults('space_day_college_student_table');
		$rdata = $row;
	}
	$templateCode .= loadView('space_day_college_student_table-ajax-cache', ['rdata' => $rdata, 'jdata' => $jdata]);

	// hook: space_day_college_student_table_dv
	if(function_exists('space_day_college_student_table_dv')) {
		$args = [];
		space_day_college_student_table_dv(($hasSelectedId ? $selectedId : FALSE), getMemberInfo(), $templateCode, $args);
	}

	return $templateCode;
}